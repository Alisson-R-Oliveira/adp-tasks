export const addition = (left: number, right: number) => left + right;

export const division = (left: number, right: number) => left / right;

export const multiplication = (left: number, right: number) => left * right;

export const remainder = (left: number, right: number) => left % right;

export const subtraction = (left: number, right: number) => left - right;
